---
description: 双屏 DPI 与经验条定位以 docs/display.md 为准
globs: docs/display.md, dpi.py, vision.py, app.py
alwaysApply: false
---

# 显示器与定位规格同步

`docs/display.md` 是本机屏幕和经验条定位的唯一规格。改采集、DPI、匹配尺度时：

1. 先读 `docs/display.md`。屏幕 1 是 2560×1600 @ 1.5×（原点 left=-2560）；屏幕 2 是 2560×1440 @ 1.0×（主屏，原点 left=0）。
2. 1366×768 放在屏幕 1、1920×1080 放在屏幕 2，都必须能定位经验条。不要只按 1080p + 100% DPI 验收。
3. 采集只走桌面 mss 小块（`grab_region` / 搜索漏斗），不要再引入绑游戏 HWND 的 Graphics Capture，也不要按标题 `EnumWindows` 找「冒险岛」做运行时采集。
4. 进程保持 DPI-unaware；物理像素走 `physical_call` + mss。不要让 Tk 跟随每监视器 DPI。
5. 文档与代码一起改。
