# 更新日志

## 010 — 2026-08-30

首个可分发绿色版：解压后双击 `MapleStoryExpStats.exe` 即可，不需要安装 Python。

### 新增

- 主窗口分「效率」和「累计」两列，浮窗挂在效率右侧，可叠在游戏上查看 /秒、/5 分等。
- 经验百分比和预计升级时间；本次秒均图吃当前列，累计图吃整场 session。
- 绿色版打包：`python scripts/pack.py` 产出免环境目录，窗口位置和经验历史写在 exe 旁边的 `data/`。

### 变更

- 用桌面小块 mss 读经验条，不读内存、不绑游戏 HWND；进程保持 DPI-unaware，拖到 150% 屏窗口不会跟着缩放。
- 经验条靠模板在桌面合成结果上定位（邻域 / 逐屏漏斗），不再按标题枚举「冒险岛」窗口做采集。
- 经验条模板按 1080p 重裁为 label / bar / ocr 三张固定名。

### 修复

- OCR 把经验读成低谷时，计算阶段跳过，不把坏点写进速率；损坏的历史行会跳过。
- 定位失败时预览图只隐藏、不把窗口高度塌掉；升级预估沿用上次有效读数。
- 定位 / 重定位显示次数和倒计时，第一次定位不算重试。
